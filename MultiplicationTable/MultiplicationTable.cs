using System;
using System.Collections.Generic;

class Program
{
    static void Main()
    {
        List<int> numbers = new List<int>();
        Console.WriteLine("Please enter an integer. Enter '.' to proceed.");
        int input = Console.ReadLine();
        numbers.Add(input);

        while (input != ".")
        /* Make it so user input gets appended to the list and when input = "." go to the next step
         * the next step is to show the avg, max, min, sum, multiplication
        of all numbers in an unordered list (just use /n at the end of a line) */
    }
}

